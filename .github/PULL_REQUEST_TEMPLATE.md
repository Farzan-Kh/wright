## What and why

What this PR changes, and why — not just what the diff shows.

## Testing

- [ ] `make test` passes
- [ ] `make lint` passes
- [ ] Tests added/updated for the change (no live API calls — see
      `CONTRIBUTING.md`)
- [ ] Manually verified with `wright smoke` against a scratch repo, if this
      touches provider/adapter behavior

## Related issue

Closes #
