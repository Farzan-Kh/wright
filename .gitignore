# Compiled binary (from `make build` / `go build`)
/wright
/dist/

# Local runtime config — may reference real repos; never commit
wright.yaml

# Local secrets
.env

# Go test binaries
*.test

# Log files
*.log